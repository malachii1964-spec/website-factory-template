{
  "name": "@malachii/trust-kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MALACHII CMA-002 Stage A trust kernel: mechanically enforced memory maturity.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "attacks": "vitest run tests/attacks",
    "properties": "vitest run tests/properties"
  }
}
