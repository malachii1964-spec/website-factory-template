{
  "name": "@malachii/sovereign-runtime-rc1",
  "version": "3.3.0-rc.1.6",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "../node_modules/.bin/tsc -p tsconfig.json",
    "typecheck": "../node_modules/.bin/tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test $(ls dist/tests/*.test.js)",
    "mutate": "npm run build && node scripts/mutate.mjs",
    "gate": "./scripts/gate.sh",
    "rehash": "./scripts/rehash.sh"
  }
}
